<% unless current_user.current_user? user %>
  <div class="follow-form">

  <% if current_user.following? user %>
    <%= render "unfollow", unfollow_user: user %>
  <% else %>
    <%= render "follow", follow_user: user %>
  <% end %>
  </div>
<% end %>
